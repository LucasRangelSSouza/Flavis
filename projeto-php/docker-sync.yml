version: "2"

options:
    verbose: true

syncs:
    files-sync: # tip: add -sync and you keep consistent names as a convention
        sync_userid: '82'
        src: './'
        # sync_strategy: 'native_osx' # not needed, this is the default now
        sync_excludes: ['.git', '.idea', '.rsync_cache']